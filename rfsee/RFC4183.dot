digraph Flow {
  rankdir=LR;
  splines=true;
  concentrate=true;   // merge parallel edges / reduce clutter
  nodesep=0.25;       // horizontal/vertical separation between nodes
  ranksep=0.35;       // separation between ranks (columns in LR)
  RFC4183 [label="RFC4183
A Suggested Scheme for DNS Resolution of
Networks and Gateways
2005", shape=ellipse, fillcolor="#e8f0ff", tooltip="This document suggests a method of using DNS to determine the network that contains a specified IP address, the netmask of that network, and the address(es) of first-hop routers(s) on that network.  This method supports variable-length subnet masks, delegation of subnets on non-octet boundaries, and multiple routers per subnet.  This memo provides information for the Internet community."]; 

  obs_by [label="obsoletes", shape=box];
  obs_by -> RFC4183;

  obs [label="obsoletes", shape=box];
  RFC4183 -> obs;

  updates [label="updates", shape=box];
  RFC4183 -> updates;

  updated_by [label="updates", shape=box];
  updated_by -> RFC4183;

  cites [label="cites", shape=box];
  RFC4183 -> cites;

  rev_cites [label="cites", shape=box];
  rev_cites -> RFC4183;
}
