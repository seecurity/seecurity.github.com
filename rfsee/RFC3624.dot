digraph Flow {
  rankdir=LR;
  splines=true;
  concentrate=true;   // merge parallel edges / reduce clutter
  nodesep=0.25;       // horizontal/vertical separation between nodes
  ranksep=0.35;       // separation between ranks (columns in LR)
  RFC3624 [label="RFC3624
The Media Gateway Control Protocol
(MGCP) Bulk Audit Package
2003", shape=ellipse, fillcolor="#e8f0ff", tooltip="The base Media Gateway Control Protocol (MGCP) includes audit commands that only allow a Call Agent to audit endpoint and/or connection state one endpoint at a time.  This document describes a new MGCP package for bulk auditing of a group of gateway endpoints.  It allows a Call Agent to determine the endpoint naming convention, the list of instantiated endpoints as well connection and endpoint state for the group of endpoints."]; 

  obs_by [label="obsoletes", shape=box];
  obs_by -> RFC3624;

  obs [label="obsoletes", shape=box];
  RFC3624 -> obs;

  updates [label="updates", shape=box];
  RFC3624 -> updates;

  updated_by [label="updates", shape=box];
  updated_by -> RFC3624;

  cites [label="cites", shape=box];
  RFC3624 -> cites;

  rev_cites [label="cites", shape=box];
  rev_cites -> RFC3624;
}
