digraph Flow {
  rankdir=LR;
  splines=true;
  concentrate=true;   // merge parallel edges / reduce clutter
  nodesep=0.25;       // horizontal/vertical separation between nodes
  ranksep=0.35;       // separation between ranks (columns in LR)
  RFC3217 [label="RFC3217
Triple-DES and RC2 Key Wrapping
2001", shape=ellipse, fillcolor="#e8f0ff", tooltip="This document specifies the algorithm for wrapping one Triple-DES key with another Triple-DES key and the algorithm for wrapping one RC2 key with another RC2 key.  This memo provides information for the Internet community."]; 

  obs_by [label="obsoletes", shape=box];
  obs_by -> RFC3217;

  obs [label="obsoletes", shape=box];
  RFC3217 -> obs;

  updates [label="updates", shape=box];
  RFC3217 -> updates;

  updated_by [label="updates", shape=box];
  updated_by -> RFC3217;

  cites [label="cites", shape=box];
  RFC3217 -> cites;

  rev_cites [label="cites", shape=box];
  rev_cites -> RFC3217;
  RFC5297 [label="RFC5297
Synthetic Initialization Vector (SIV)
Authenticated Encryption Using the
Advanced Encryption Standard (AES)
2008", shape=ellipse, URL="RFC5297.html", target="_top", tooltip="This memo describes SIV (Synthetic Initialization Vector), a block cipher mode of operation.  SIV takes a key, a plaintext, and multiple variable-length octet strings that will be authenticated but not encrypted.  It produces a ciphertext having the same length as the plaintext and a synthetic initialization vector.  Depending on how it is used, SIV achieves either the goal of deterministic authenticated encryption or the goal of nonce-based, misuse-resistant authenticated encryption.  This memo provides information for the Internet community."];
  RFC5297 -> rev_cites;
}
